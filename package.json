{
  "name": "http-vs-https-demo",
  "version": "1.0.0",
  "description": "HTTP vs HTTPS Demo",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "generate-cert": "openssl req -nodes -new -x509 -keyout private.key -out certificate.crt -days 365 -subj '/CN=localhost'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.1.14"
  }
}
